name: Deploy AI Service to AWS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Create deployment bundle
        run: |
          zip -r deploy.zip . -x ".git/*" "*.pyc" "__pycache__/*" ".env*" "tests/*" "2connect_matching_analysis/*"

      - name: Upload to S3
        run: |
          aws s3 cp deploy.zip s3://2connect-dev-backend-artifacts/ai-service/deploy.zip

      - name: Deploy via CodeDeploy
        run: |
          aws deploy create-deployment \
            --application-name 2connect \
            --deployment-group-name ai-service \
            --s3-location bucket=2connect-dev-backend-artifacts,key=ai-service/deploy.zip,bundleType=zip \
            --file-exists-behavior OVERWRITE
